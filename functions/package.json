{
    "name": "smartstock-functions",
    "description": "Cloud Functions do SmartStock (chatRespond com Vertex AI)",
    "private": true,
    "type": "commonjs",
    "main": "lib/index.js",
    "engines": {
        "node": "20"
    },
    "scripts": {
        "build": "tsc",
        "serve": "npm run build && firebase emulators:start --only functions",
        "deploy": "npm run build && firebase deploy --only functions"
    },
    "dependencies": {
        "@google-cloud/vertexai": "^1.8.0",
        "firebase-admin": "^12.6.0",
        "firebase-functions": "^5.1.1",
        "cors": "^2.8.5"
    },
    "devDependencies": {
        "@types/node": "^20.16.11",
        "typescript": "^5.6.3"
    }
}