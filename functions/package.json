{
    "name": "smartstock-functions",
    "private": true,
    "engines": { "node": "20" },
    "main": "lib/index.js",
    "scripts": {
        "build": "tsc",
        "clean": "rimraf lib",
        "deploy": "npm run build && firebase deploy --only functions:actCall"
    },
    "dependencies": {
        "firebase-admin": "^12.6.0",
        "firebase-functions": "^5.1.1"
    },
    "devDependencies": {
        "typescript": "^5.6.3",
        "rimraf": "^5.0.5"
    }
}